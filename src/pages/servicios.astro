---
/**
 * servicios.astro
 * Página de servicios para Barbería Premium
 */

export const prerender = true;

import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import WhatsAppButton from '../components/WhatsAppButton.astro';

const services = [
  {
    name: 'Corte Clásico',
    price: '$8.000',
    description: 'Técnica tradicional con acabado a tijera y máquina para un look impecable día a día.',
    icon: '✂️',
    features: ['Lavado express', 'Peinado de cortesía', 'Recomendación de estilo'],
  },
  {
    name: 'Degradado Moderno',
    price: '$10.000',
    description: 'Fade preciso con transición perfecta y definición en patillas, nuca y contornos.',
    icon: '🔥',
    features: ['Diseño de líneas', 'Acabado con pomadas premium', 'Asesoría de mantenimiento'],
  },
  {
    name: 'Afeitado con Toalla Caliente',
    price: '$12.000',
    description: 'Ritual clásico con toallas aromáticas, espuma caliente y navaja profesional.',
    icon: '🪒',
    features: ['Doble toalla caliente', 'After shave calmante', 'Masaje facial ligero'],
  },
  {
    name: 'Coloración y Tinte',
    price: '$15.000',
    description: 'Cobertura natural o tonos atrevidos con productos profesionales y asesoría experta.',
    icon: '🎨',
    features: ['Diagnóstico personalizado', 'Productos sin amoníaco', 'Sellado con tratamiento'],
  },
];

const basePath = (import.meta.env.BASE_URL || '/').replace(/\/$/, '');

const resolveLink = (href: string) => {
  if (!href) return '#';
  const isExternal = /^https?:\/\//.test(href) || href.startsWith('#') || href.startsWith('mailto:') || href.startsWith('tel:');
  if (isExternal) return href;
  const path = href.startsWith('/') ? href : `/${href}`;
  return `${basePath}${path}`;
};
---

<BaseLayout
  title="Nuestros Servicios | Barbería Premium Temuco"
  description="Descubre los servicios profesionales de Barbería Premium: cortes modernos, degradados, afeitado con toalla caliente y coloración especializada."
>
  <Navbar />

  <main class="bg-neutral-950 text-white">
    <section class="relative overflow-hidden py-28">
      <div class="absolute inset-0 bg-gradient-to-br from-yellow-500/10 via-transparent to-yellow-500/10 pointer-events-none"></div>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-3xl" data-aos="fade-up">
          <p class="inline-flex items-center gap-2 rounded-full border border-yellow-500/30 bg-yellow-500/10 px-4 py-2 text-sm font-semibold text-yellow-200">
            ✂️ Servicios profesionales
          </p>
          <h1 class="mt-6 font-heading text-4xl sm:text-5xl md:text-6xl font-bold leading-tight">
            Nuestros Servicios
          </h1>
          <p class="mt-4 text-lg text-neutral-300">
            Experiencia premium con atención personalizada, productos de alta calidad y un ambiente pensado para tu confort.
          </p>
        </div>
      </div>
    </section>

    <section class="pb-24">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid gap-8 md:grid-cols-2">
          {services.map((service, index) => (
            <article
              class="group relative overflow-hidden rounded-3xl border border-yellow-500/15 bg-neutral-900/70 p-8 shadow-xl transition-transform duration-300 hover:-translate-y-1.5 hover:border-yellow-500/40 hover:shadow-2xl hover:shadow-black/30"
              data-aos="fade-up"
              data-aos-delay={index * 100}
            >
              <div class="absolute inset-0 bg-gradient-to-br from-yellow-500/5 via-transparent to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100"></div>
              <div class="relative z-10 space-y-6">
                <div class="flex items-center justify-between">
                  <span class="text-4xl">{service.icon}</span>
                  <span class="rounded-full border border-yellow-500/30 bg-yellow-500/10 px-4 py-1 text-sm font-semibold text-yellow-300">
                    {service.price}
                  </span>
                </div>

                <div>
                  <h2 class="font-heading text-2xl font-semibold text-white">{service.name}</h2>
                  <p class="mt-3 text-sm text-neutral-300">{service.description}</p>
                </div>

                <ul class="space-y-2 text-sm text-neutral-300">
                  {service.features.map((feature) => (
                    <li class="flex items-center gap-2">
                      <span class="h-2 w-2 rounded-full bg-yellow-500"></span>
                      {feature}
                    </li>
                  ))}
                </ul>

                <a
                  href={resolveLink('/reservas')}
                  class="inline-flex items-center justify-center rounded-full border border-yellow-500/40 bg-yellow-500/10 px-6 py-3 text-sm font-semibold text-yellow-200 transition-all duration-300 hover:bg-yellow-500 hover:text-neutral-900"
                >
                  Reservar
                </a>
              </div>
            </article>
          ))}
        </div>

        <div class="mt-20 grid gap-10 lg:grid-cols-[minmax(0,1.2fr)_minmax(0,0.8fr)] items-center rounded-3xl border border-yellow-500/15 bg-neutral-900/60 px-8 py-10" data-aos="fade-up">
          <div>
            <h2 class="font-heading text-3xl font-semibold text-white">Experiencia Barbería Premium</h2>
            <p class="mt-4 text-neutral-300">
              Todos nuestros servicios incluyen asesoría personalizada, productos profesionales y recomendaciones de cuidado para mantener tu estilo en casa.
            </p>
          </div>
          <div class="grid gap-4 sm:grid-cols-2">
            {[
              'Productos de barbería de alta gama',
              'Higiene y protocolos sanitarios certificados',
              'Ambiente cómodo con bebidas de cortesía',
              'Programa de fidelización y descuentos especiales',
            ].map((item) => (
              <div class="rounded-2xl border border-yellow-500/20 bg-white/5 px-4 py-4 text-sm text-neutral-200">
                {item}
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>
  </main>

  <WhatsAppButton />
  <Footer />
</BaseLayout>
